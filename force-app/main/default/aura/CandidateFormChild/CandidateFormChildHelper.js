({
	helperMethod : function(component, event, helper) {
  /*      alert("hello helper");
        var action = component.get("c:getId");
        var posiName = component.find("select").get("v.value");
        action.setparam({"myname":posiName});
        action.setCallBack(this, function(response){
            var state = response.getState();
            if(state === 'SUCESS'){
                component.set("v.candidate.Position_Applied__c",response.getReturnValue());
            }
            else{
                alert(response.getErrorMessage());
            }
        })
        console.log("helper");
        $A.enqueueAction(action);*/
	}
})